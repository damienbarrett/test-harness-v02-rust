[package]
name = "tasks"
version = "0.1.0"
edition = "2021"
description = "Task entity operations"

[dependencies]
serde = { version = "1.0", features = ["derive"] }

[dev-dependencies]
serde_json = "1.0"
